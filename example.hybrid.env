# Local DB (compose PostgreSQL), cloud SEPEX + S3.
# Copy to .env before running `just up-hybrid`.

# Pipeline database (local compose)
POSTGRES_USER=twodfim
POSTGRES_PASSWORD=twodfim
POSTGRES_DB=twodfim
POSTGRES_PORT=5432
POSTGRES_HOST=localhost

# S3 (real AWS, not MinIO)
AWS_ACCESS_KEY_ID=<aws-access-key-id>
AWS_SECRET_ACCESS_KEY=<aws-secret-access-key>
AWS_SESSION_TOKEN=<aws-session-token>
AWS_REGION=us-east-1
# AWS_REQUEST_PAYER=requester
ARTIFACTS_S3_BUCKET=<artifacts-bucket-name>
MAJOR_VERSION=1

# Cloud SEPEX (port-forwarded via SSM to localhost:9090)
SEPEX_URL=http://localhost:9090

# Cloud Batch runs on GPU SPOT instances
GPU_AVAILABLE=true
